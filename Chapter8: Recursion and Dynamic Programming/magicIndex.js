// A magic index in an array A[0... n-1] is defined to be an index such that A[i] = i. Given a sorted array of distinct integers, write a binary search method to find a magic index, if one exists, in array A.

const magicIndex = (array) => {
  let start = array[0];
  let end = array
}